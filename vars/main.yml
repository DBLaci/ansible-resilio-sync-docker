---

resilio_sync_config:
  device_name: "{{ resilio_sync_device_name }}"
  listening_port: 55555
  storage_path: "/mnt/sync/config"
  use_upnp: false
  download_limit: 0
  upload_limit: 0
  shared_folders: "{{ resilio_sync_shared_folders }}"
